export function GenericModulePage({ title }) {
  return (
    <section>
      <h2>Módulo: {title}</h2>
      <p>Este módulo está preparado para receber tabelas, formulários e gráficos específicos.</p>
    </section>
  );
}
